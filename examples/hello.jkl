# declare the types
let copy Slice = @Slice.0;
let copy Nat = @Nat.0;

let copy U64 = Nat(64);
let copy Char = Nat(8);
let copy CString = Slice(Char);

# declare the external functions
let puts = extern "puts" CString -> struct {};

let main = argc: U64, argv: Slice(CString) => U64 
:: {
    puts(@array_to_slice(Char, 14)("Hello, world!\n"));
    0
};